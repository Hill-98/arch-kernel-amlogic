name: Build Kernel CI
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux
    steps:
    - name: Build Kernel
      uses: actions/checkout@v2
      run: |
        ./make.sh